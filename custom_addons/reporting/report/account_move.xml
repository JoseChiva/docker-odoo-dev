<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="report_event_registration_badge_account_move" model="ir.actions.report">
        <field name="name">account_move_auren_format</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">reporting.report_account_move_format_prin</field> <!-- Es el id del template con el prefijo del nombre del modulo-->
        <field name="report_file">reporting.report_account_move_format_prin</field>
        <field name="print_report_name">'Factura - %s' % (object.name).replace('/','')</field>
        <field name="binding_model_id" eval="False"/>
    </record>
</odoo>